# Tasks

- [x] Draft the proposal outlining scope and capabilities for Target Lists and Targets.
- [x] Record design notes on list composition, list types, membership storage, compliance flags, and campaign interactions.
- [x] Write the Target Lists specification covering creation modes, segmentation, classification, membership tracking, and list operations (merge, dedupe, archive, import/export).
- [x] Write the Targets specification covering minimal data, statuses, conversions, opt-outs/DNC, source tracking, imports, deduplication, and activity history.
- [x] Note that `openspec` CLI validation is unavailable in this environment and perform manual checks instead.
