@props(['url'])
<tr>
    <td class="header">
        <a href="{{ $url }}" style="display: inline-block;">
            <img src="{{ brand_logo_white_asset() }}" class="logo" alt="{{ brand_name() }} Logo">
        </a>
    </td>
</tr>
